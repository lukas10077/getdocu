// Platzhalter — die tatsächliche Tool-Route liegt unter app/[locale]/tools/[slug]/.
// Diese Datei existiert nur, damit die im Auftrag vorgegebene Ordnerstruktur
// (app/tools/mietbewerbung etc.) im Repo sichtbar ist. Sie wird in einem späteren
// Schritt durch die volle Formular-Implementierung unter [locale] ersetzt/verlinkt.
export default function Placeholder() {
  return null;
}
